---
phase: 05-launch-assets
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified: [README.md]
autonomous: true
requirements: [PROMO-02]

must_haves:
  truths:
    - "GitHub repo has all 7 required topics"
    - "Repo homepage URL is set to https://hamtools.cc"
    - "README contains a clickable video thumbnail linking to the YouTube demo"
    - "README contains CI, Docker Pulls, and License badges"
  artifacts:
    - path: "README.md"
      provides: "Video embed and badges"
      contains: "img.youtube.com"
  key_links:
    - from: "README.md"
      to: "YouTube video"
      via: "clickable thumbnail image"
      pattern: "img\\.youtube\\.com/vi/"
    - from: "GitHub repo topics"
      to: "GitHub search"
      via: "topic metadata"
      pattern: "ham-radio.*cat-control.*yaesu"
---

<objective>
Add GitHub topics to the cat-ftx1v2 repo, set the homepage URL, and update the README with a video embed and status badges.

Purpose: Makes the repo discoverable via GitHub topic search and gives visitors an immediate visual demo without leaving the page.
Output: Updated README.md with video thumbnail and badges; repo metadata configured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-launch-assets/05-RESEARCH.md
@README.md
</context>

<interfaces>
From .planning/phases/05-launch-assets/05-RESEARCH.md:

GitHub topics API (bulk replace):
```bash
gh api repos/mapoby/cat-ftx1v2/topics \
  --method PUT \
  -H "Accept: application/vnd.github+json" \
  -f 'names[]=ham-radio' \
  -f 'names[]=cat-control' \
  -f 'names[]=yaesu' \
  -f 'names[]=yaesu-ftx1' \
  -f 'names[]=web-serial' \
  -f 'names[]=nuxt' \
  -f 'names[]=vue'
```

README video embed pattern:
```markdown
[![CAT FTX-1 Controller — Live demo](https://img.youtube.com/vi/VIDEO_ID/hqdefault.jpg)](https://www.youtube.com/watch?v=VIDEO_ID)
```

Badge patterns:
```markdown
![CI](https://img.shields.io/github/actions/workflow/status/mapoby/cat-ftx1v2/azure-deploy.yml?branch=main&label=CI)
![Docker Pulls](https://img.shields.io/docker/pulls/mapoby/cat-ftx1)
![License](https://img.shields.io/github/license/mapoby/cat-ftx1v2)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Set GitHub repo topics and homepage URL</name>
  <files></files>
  <read_first>.planning/phases/05-launch-assets/05-RESEARCH.md</read_first>
  <action>
Run the following gh CLI commands to set repo metadata:

1. Set all 7 topics at once (bulk replace, not one-by-one):
```bash
gh api repos/mapoby/cat-ftx1v2/topics \
  --method PUT \
  -H "Accept: application/vnd.github+json" \
  -f 'names[]=ham-radio' \
  -f 'names[]=cat-control' \
  -f 'names[]=yaesu' \
  -f 'names[]=yaesu-ftx1' \
  -f 'names[]=web-serial' \
  -f 'names[]=nuxt' \
  -f 'names[]=vue'
```

2. Set homepage URL:
```bash
gh repo edit mapoby/cat-ftx1v2 --homepage "https://hamtools.cc"
```

Do NOT use `--add-topic` one at a time — use the bulk PUT API call as shown above.
  </action>
  <verify>
    <automated>gh api repos/mapoby/cat-ftx1v2/topics | grep -c "ham-radio" && gh api repos/mapoby/cat-ftx1v2 --jq '.homepage'</automated>
  </verify>
  <acceptance_criteria>
    - `gh api repos/mapoby/cat-ftx1v2/topics` returns exactly 7 topics: ham-radio, cat-control, yaesu, yaesu-ftx1, web-serial, nuxt, vue
    - `gh api repos/mapoby/cat-ftx1v2 --jq '.homepage'` returns "https://hamtools.cc"
  </acceptance_criteria>
  <done>Repo has 7 topics and homepage URL set to https://hamtools.cc</done>
</task>

<task type="auto">
  <name>Task 2: Add video embed and badges to README.md</name>
  <files>README.md</files>
  <read_first>README.md, .planning/phases/05-launch-assets/05-01-SUMMARY.md</read_first>
  <action>
Read the VIDEO_ID from plan 05-01 SUMMARY (provided by the user during that plan's execution).

Edit README.md to add:

1. **Badges** — insert immediately after the H1 title line (`# Yaesu FTX-1 radio WEB based remote control v2`), before the description paragraph:
```markdown
![CI](https://img.shields.io/github/actions/workflow/status/mapoby/cat-ftx1v2/azure-deploy.yml?branch=main&label=CI)
![Docker Pulls](https://img.shields.io/docker/pulls/mapoby/cat-ftx1)
![License](https://img.shields.io/github/license/mapoby/cat-ftx1v2)
```

2. **Video thumbnail** — replace the existing screenshot image line (`![FTX-1 Console Screenshot]...`) with a clickable YouTube thumbnail:
```markdown
[![CAT FTX-1 Controller — Live demo](https://img.youtube.com/vi/VIDEO_ID/hqdefault.jpg)](https://www.youtube.com/watch?v=VIDEO_ID)
```
Replace `VIDEO_ID` with the actual YouTube video ID from plan 05-01's SUMMARY.

Use `hqdefault.jpg` (480x360, always available immediately) — NOT `maxresdefault.jpg` which may not exist for new videos.

Do NOT change any other content in the README. Only add badges after the title and replace the screenshot with the video thumbnail.
  </action>
  <verify>
    <automated>grep -c "img.youtube.com/vi/" README.md && grep -c "shields.io" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains a line matching `[![CAT FTX-1 Controller — Live demo](https://img.youtube.com/vi/` followed by a real VIDEO_ID
    - README.md contains exactly 3 shields.io badge lines (CI, Docker Pulls, License)
    - The old screenshot line (`![FTX-1 Console Screenshot]...`) is removed
    - No other README content is modified
  </acceptance_criteria>
  <done>README has clickable video thumbnail and three status badges</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External image CDNs | shields.io and img.youtube.com serve badge/thumbnail images |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | T (Tampering) | shields.io badges | accept | Third-party service; badges are informational only, not security-relevant |
| T-05-04 | D (Denial of Service) | img.youtube.com | accept | If YouTube CDN is down, thumbnail shows broken image — no functional impact |
</threat_model>

<verification>
- `gh api repos/mapoby/cat-ftx1v2/topics --jq '.names'` returns all 7 topics
- `gh api repos/mapoby/cat-ftx1v2 --jq '.homepage'` returns `https://hamtools.cc`
- `grep "img.youtube.com" README.md` finds the video embed
- `grep "shields.io" README.md` finds 3 badge lines
</verification>

<success_criteria>
Repo is discoverable via GitHub topic search for "ham-radio", "yaesu", "web-serial", etc. Visitors see a demo video thumbnail and status badges immediately upon opening the repo page.
</success_criteria>

<output>
After completion, create `.planning/phases/05-launch-assets/05-02-SUMMARY.md`
</output>
