---
phase: 04-infrastructure-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - Dockerfile
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02

must_haves:
  truths:
    - "magic-regexp is absent from package.json dependencies"
    - "package-lock.json exists in the repo and does not reference magic-regexp"
    - "Dockerfile copies package-lock.json and uses npm ci (not npm install)"
    - "docker build fails if package-lock.json is absent or inconsistent with package.json"
    - "README.md no longer mentions magic-regexp as an active dependency"
  artifacts:
    - path: "package.json"
      provides: "dependency manifest without magic-regexp"
      contains: "no magic-regexp entry"
    - path: "package-lock.json"
      provides: "committed lockfile for reproducible builds"
    - path: "Dockerfile"
      provides: "reproducible build stage using npm ci"
      contains: "npm ci"
  key_links:
    - from: "Dockerfile"
      to: "package-lock.json"
      via: "COPY package.json package-lock.json ./ then npm ci"
      pattern: "COPY package\\.json package-lock\\.json"
---

<objective>
Remove the unused magic-regexp dependency, regenerate package-lock.json without it, and switch the Dockerfile builder stage to npm ci so Docker builds are reproducible and fail fast on lockfile drift.

Purpose: INFRA-01 requires a committed lockfile and npm ci in the Dockerfile. INFRA-02 requires magic-regexp to be gone. These two changes are coupled — magic-regexp must be removed before the lockfile is regenerated.

Output: package.json without magic-regexp, a fresh package-lock.json without magic-regexp, a Dockerfile that uses npm ci.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove magic-regexp and regenerate package-lock.json</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json — see current dependency list before modification
    - package-lock.json — confirm magic-regexp is present before removal
    - README.md — find the magic-regexp reference in the Tech Stack table (line 22) to remove in Task 2
  </read_first>
  <action>
    Run `npm uninstall magic-regexp` from the project root. This command:
    - Removes `"magic-regexp": "^0.11.0"` from the `dependencies` section of package.json
    - Regenerates package-lock.json to remove magic-regexp and all its transitive dependencies
    - Leaves all other dependencies (nuxt, @types/node, vitest, etc.) intact

    Do NOT manually edit package.json — use the npm command to ensure lockfile consistency.

    After uninstall, verify:
    - `grep "magic-regexp" package.json` returns nothing
    - `grep "magic-regexp" package-lock.json` returns nothing
    - `npm ls` exits 0 with no missing dependency warnings
  </action>
  <verify>
    <automated>grep -c "magic-regexp" package.json; echo "exit:$?"</automated>
  </verify>
  <done>package.json has no magic-regexp entry; package-lock.json has no magic-regexp entry; npm ls exits 0</done>
</task>

<task type="auto">
  <name>Task 2: Switch Dockerfile to npm ci and remove stale README reference</name>
  <files>Dockerfile, README.md</files>
  <read_first>
    - Dockerfile — see current build stage (line 7 is `RUN npm install --prefer-offline`)
    - README.md — locate the Tech Stack table row referencing magic-regexp (line 22: "Pattern matching | magic-regexp v0.11 for CAT response parsing")
  </read_first>
  <action>
    Make two edits:

    **Dockerfile change** — Replace lines 6-7 (the COPY and install lines in the builder stage) with:
    ```dockerfile
    COPY package.json package-lock.json ./
    RUN npm ci
    ```
    The current line 6 is `COPY package.json ./` — it must be updated to also copy the lockfile.
    The current line 7 is `RUN npm install --prefer-offline` — replace with `RUN npm ci`.
    The `npm ci` command: (1) reads package-lock.json exactly, (2) fails with a non-zero exit if package.json and package-lock.json are out of sync, (3) performs a clean install (no npm cache reuse). This is the reproducible-build standard.

    **README.md change** — In the Tech Stack table under "Frontend", remove the row:
    ```
    | Pattern matching | [magic-regexp](https://github.com/danielroe/magic-regexp) v0.11 for CAT response parsing |
    ```
    This row is stale after INFRA-02. The table should only list active dependencies.

    Do not change any other lines in either file.
  </action>
  <verify>
    <automated>grep -n "npm ci" Dockerfile && grep -v "magic-regexp" README.md | grep -c "magic-regexp"; echo "exit:$?"</automated>
  </verify>
  <done>Dockerfile line 6 is `COPY package.json package-lock.json ./`; Dockerfile line 7 is `RUN npm ci`; README.md contains no reference to magic-regexp</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → Dockerfile build | npm ci reads a committed lockfile; no live resolution at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering / Supply Chain | Dockerfile npm install | mitigate | Switch to npm ci with committed lockfile — build fails if lockfile drifts from package.json |
| T-04-02 | Information Disclosure | magic-regexp in production bundle | accept | Dependency is unused; removal eliminates it from bundle analysis; no PII involved |
</threat_model>

<verification>
1. `grep "magic-regexp" package.json` — must return empty (exit 1)
2. `grep "magic-regexp" package-lock.json` — must return empty (exit 1)
3. `grep "npm ci" Dockerfile` — must match
4. `grep "package-lock.json" Dockerfile` — must match (COPY line)
5. `grep "magic-regexp" README.md` — must return empty (exit 1)
6. `npm ci --dry-run` from project root exits 0 (lockfile and package.json are consistent)
</verification>

<success_criteria>
- magic-regexp absent from package.json and package-lock.json
- Dockerfile copies package-lock.json before running npm ci
- Dockerfile has no npm install line
- README.md Tech Stack table does not mention magic-regexp
- `npm ci --dry-run` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/04-infrastructure-hardening/04-01-SUMMARY.md`
</output>
