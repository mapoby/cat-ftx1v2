---
phase: 03-data-integrity-memory-operations
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - pages/index.vue
autonomous: true
requirements:
  - DATA-03

must_haves:
  truths:
    - "Clicking 'Delete Selected' shows a confirmation modal with the slot count and persistent disclosure before any deletion occurs"
    - "The Wipe All modal cannot be dismissed by clicking the backdrop or the X button — only Cancel or WIPE ALL close it"
    - "Both modals state that deletion overwrites with blank values and does not remove the slot number from the radio"
    - "Confirming delete in the delete-selected modal triggers the existing deleteSelectedFromRadio() logic"
  artifacts:
    - path: "pages/index.vue"
      provides: "deleteDisclosureDialog ref and modal template"
      contains: "deleteDisclosureDialog"
    - path: "pages/index.vue"
      provides: "Wipe All modal with X button removed and backdrop click removed"
  key_links:
    - from: "Delete Selected button"
      to: "deleteDisclosureDialog ref"
      via: "@click sets deleteDisclosureDialog = selectedSlots.length"
      pattern: "deleteDisclosureDialog"
    - from: "deleteDisclosureDialog modal confirm button"
      to: "deleteSelectedFromRadio()"
      via: "@click handler on confirm button"
      pattern: "deleteSelectedFromRadio"
    - from: "Wipe All modal backdrop"
      to: "no dismiss"
      via: "@click.self removed from backdrop div"
      pattern: "wipeAllDialog"
---

<objective>
Make delete and wipe-all operations require explicit acknowledgement with a persistent modal disclosing the protocol's blank-overwrite semantics.

Purpose: Operators must understand that the FTX-1 has no delete command — slots are overwritten with blank defaults, not removed. Dismissal without acknowledgement is prevented (DATA-03).
Output: A new delete-selected disclosure modal in pages/index.vue; the existing Wipe All modal hardened to remove its X close button and backdrop-click dismiss path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-data-integrity-memory-operations/03-RESEARCH.md
@.planning/phases/03-data-integrity-memory-operations/03-01-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From pages/index.vue — existing Wipe All modal (lines 1019-1037):
```html
<!-- ── Wipe All Memory confirmation dialog ── -->
<Teleport to="body">
  <div v-if="wipeAllDialog" class="tone-modal-backdrop" @click.self="wipeAllDialog = false">
    <div class="tone-modal wipe-confirm-modal" role="dialog" aria-modal="true" aria-label="Wipe All Memory">
      <div class="tone-modal-header wipe-confirm-header">
        <span class="tone-modal-title">⚠ WARNING</span>
        <button class="tone-modal-close" @click="wipeAllDialog = false" aria-label="Close">✕</button>
      </div>
      <div class="wipe-confirm-body">
        <p>This will overwrite <strong>all {{ channelListRows.length }} channels</strong> in radio memory with blank defaults (29 MHz, USB, no SQL, empty tag).</p>
        <p>The radio has no delete command — slots will remain but contain no useful data. This cannot be undone.</p>
      </div>
      <div class="wipe-confirm-actions">
        <button class="btn btn-sm" @click="wipeAllDialog = false">Cancel</button>
        <button class="btn btn-sm btn-wipe" @click="wipeAllMemory">WIPE ALL</button>
      </div>
    </div>
  </div>
</Teleport>
```

From pages/index.vue — Delete Selected button (lines 533-539):
```html
<button
  v-if="selectedSlots.length > 0"
  class="btn btn-sm btn-del chlist-action-btn"
  :disabled="chListDeleting || !state.connected"
  @click="deleteSelectedFromRadio"
  title="Overwrite selected memory slots with blank data on the radio"
>{{ chListDeleting ? `Deleting…` : `Delete Selected (${selectedSlots.length})` }}</button>
```

From pages/index.vue — deleteSelectedFromRadio (lines 2338-2352):
```typescript
async function deleteSelectedFromRadio() {
  if (chListDeleting.value || selectedSlots.value.length === 0) return
  chListDeleting.value = true
  const slots = [...selectedSlots.value]
  try {
    for (const slot of slots) {
      await deleteMemorySlot(slot).catch((e: any) => { lastError.value = e.message })
    }
    channelListRows.value = channelListRows.value.filter(r => !slots.includes(r.slot))
    saveChannelList()
  } finally {
    selectedSlots.value = []
    chListDeleting.value = false
  }
}
```

From pages/index.vue — ref declarations block (around line 1134):
```typescript
const wipeAllDialog   = ref(false)
const chListWiping    = ref(false)
// ADD after wipeAllDialog:
// const deleteDisclosureDialog = ref(false)
```

Existing modal CSS classes used (for reference, do not reinvent):
- `.tone-modal-backdrop` — full-screen overlay
- `.tone-modal` — modal box
- `.tone-modal-header` — header row
- `.tone-modal-title` — title text
- `.tone-modal-close` — X close button (REMOVE from wipe modal, DO NOT add to delete modal)
- `.wipe-confirm-body` — body paragraph area
- `.wipe-confirm-actions` — action button row
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden the Wipe All modal — remove backdrop dismiss and X button</name>
  <files>pages/index.vue</files>
  <read_first>
    - pages/index.vue lines 1019-1037 (existing Wipe All modal template)
  </read_first>
  <action>
In the Wipe All modal template (around lines 1019-1037), make two changes:

**Change 1 — Remove backdrop-click dismiss.**
Remove `@click.self="wipeAllDialog = false"` from the `.tone-modal-backdrop` div. The backdrop div becomes:
```html
  <div v-if="wipeAllDialog" class="tone-modal-backdrop">
```

**Change 2 — Remove the X close button.**
Delete the entire `<button class="tone-modal-close" ...>✕</button>` element from the modal header. The header becomes:
```html
      <div class="tone-modal-header wipe-confirm-header">
        <span class="tone-modal-title">⚠ WARNING</span>
      </div>
```

The Cancel button and WIPE ALL button in `.wipe-confirm-actions` remain unchanged — these are the only two ways to close the modal.

No other changes to the Wipe All modal.
  </action>
  <verify>
    <automated>grep -n "tone-modal-close\|@click.self.*wipeAllDialog" c:/GitHub/cat-ftx1v2/pages/index.vue</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@click.self.*wipeAllDialog" pages/index.vue` outputs `0`
    - `grep -n "tone-modal-close" pages/index.vue` outputs no lines (the X button is gone)
    - `grep -c "wipeAllDialog = false" pages/index.vue` outputs `1` — only the Cancel button remains as a close path (the wipeAllMemory function also sets it to false, but that's inside the JS function not in a click handler in the template; verify the template only has Cancel)
    - The wipe modal structure still contains the disclosure text: `grep -c "no delete command" pages/index.vue` outputs at least `1`
  </acceptance_criteria>
  <done>Wipe All modal can only be closed via the Cancel button or WIPE ALL — no backdrop click, no X button</done>
</task>

<task type="auto">
  <name>Task 2: Add Delete Selected confirmation modal</name>
  <files>pages/index.vue</files>
  <read_first>
    - pages/index.vue lines 533-539 (Delete Selected button in toolbar)
    - pages/index.vue lines 1134-1137 (ref declarations: wipeAllDialog, chListWiping)
    - pages/index.vue lines 1019-1037 (Wipe All modal — use as structural model)
    - pages/index.vue lines 2338-2352 (deleteSelectedFromRadio function)
  </read_first>
  <action>
**Step 1 — Add ref declaration.**
In the ref declarations block, immediately after `const wipeAllDialog = ref(false)`, add:
```typescript
const deleteDisclosureDialog = ref(false)
```

**Step 2 — Change Delete Selected button to open modal instead of calling deleteSelectedFromRadio directly.**
In the toolbar template (lines 533-539), change the Delete Selected button's `@click` handler from:
```html
  @click="deleteSelectedFromRadio"
```
to:
```html
  @click="deleteDisclosureDialog = true"
```
The button text, disabled condition, and class remain unchanged.

**Step 3 — Add the delete disclosure modal to the template.**
Immediately after the closing `</Teleport>` of the Wipe All modal (after line 1037, before `</div>` closing the app div at line 1039), add:

```html
    <!-- ── Delete Selected confirmation dialog ── -->
    <Teleport to="body">
      <div v-if="deleteDisclosureDialog" class="tone-modal-backdrop">
        <div class="tone-modal wipe-confirm-modal" role="dialog" aria-modal="true" aria-label="Delete Selected Channels">
          <div class="tone-modal-header wipe-confirm-header">
            <span class="tone-modal-title">⚠ WARNING</span>
          </div>
          <div class="wipe-confirm-body">
            <p>This will overwrite <strong>{{ selectedSlots.length }} selected slot{{ selectedSlots.length === 1 ? '' : 's' }}</strong> in radio memory with blank defaults (29 MHz, USB, no SQL, empty tag).</p>
            <p>The radio has no delete command — slots will remain but contain no useful data. This cannot be undone.</p>
          </div>
          <div class="wipe-confirm-actions">
            <button class="btn btn-sm" @click="deleteDisclosureDialog = false">Cancel</button>
            <button class="btn btn-sm btn-del" @click="deleteDisclosureDialog = false; deleteSelectedFromRadio()">DELETE SELECTED</button>
          </div>
        </div>
      </div>
    </Teleport>
```

Key properties of this modal:
- No `@click.self` on the backdrop (persistent — cannot be dismissed by clicking outside)
- No X close button in the header
- Disclosure text: "The radio has no delete command — slots will remain but contain no useful data."
- Confirm button calls `deleteDisclosureDialog = false` then `deleteSelectedFromRadio()` — sets dialog to false first so it doesn't block the async operation
- Cancel button closes without acting
  </action>
  <verify>
    <automated>grep -c "deleteDisclosureDialog" c:/GitHub/cat-ftx1v2/pages/index.vue</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "deleteDisclosureDialog" pages/index.vue` outputs at least `4` (ref decl + button click + v-if + confirm handler)
    - `grep -n "@click=\"deleteSelectedFromRadio\"" pages/index.vue` outputs `0` — Delete Selected button no longer calls deleteSelectedFromRadio directly
    - `grep -n "@click=\"deleteDisclosureDialog = true\"" pages/index.vue` outputs `1` — Delete Selected button opens the modal
    - The delete modal template contains: `no delete command`
    - The delete modal template has no `@click.self` and no `tone-modal-close` button
    - `grep -c "deleteDisclosureDialog = false; deleteSelectedFromRadio" pages/index.vue` outputs `1` — the confirm handler
    - `npm run generate` exits 0
  </acceptance_criteria>
  <done>Clicking Delete Selected opens a persistent modal showing slot count and blank-overwrite disclosure; Cancel closes without acting; DELETE SELECTED triggers deleteSelectedFromRadio()</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → delete action | User must explicitly confirm before destructive radio write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | deleteSelectedFromRadio click path | mitigate | Modal gate added — button no longer calls deletion function directly |
| T-03-05 | Tampering | Wipe All modal dismiss | mitigate | Backdrop click and X button removed — no accidental dismiss path |
| T-03-06 | Denial of Service | Modal blocking UI | accept | Modal is intentional friction for destructive operation; dismiss via Cancel always available |
</threat_model>

<verification>
1. `grep -c "@click.self.*wipeAllDialog" pages/index.vue` → `0`
2. `grep -c "tone-modal-close" pages/index.vue` → `0`
3. `grep -c "deleteDisclosureDialog" pages/index.vue` → at least `4`
4. `grep -n "@click=\"deleteSelectedFromRadio\"" pages/index.vue` → `0`
5. Wipe All modal disclosure text still present: `grep -c "no delete command" pages/index.vue` → `1`
6. Delete modal disclosure text present: second occurrence of `no delete command` via grep -c → `2` total after this plan
7. `npm run generate` exits 0
</verification>

<success_criteria>
- Wipe All modal: no X button, no backdrop-click dismiss, only Cancel and WIPE ALL (DATA-03)
- Delete Selected: opens disclosure modal before any deletion; modal is persistent; DELETE SELECTED calls deleteSelectedFromRadio() (DATA-03)
- Both modals state slot-not-removed-from-radio semantics
- No TypeScript compile errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-data-integrity-memory-operations/03-02-SUMMARY.md`
</output>
